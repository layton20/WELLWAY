meta {
  name: Refresh Token
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/refresh
  body: json
  auth: none
}

body:json {
  {
    "accessToken": "{{accessToken}}",
    "refreshToken": "{{refreshToken}}"
  }
}
